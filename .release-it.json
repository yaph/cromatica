{
  "git": {
    "commitMessage": "Release ${version}",
    "changelog": "git log --pretty=format:\"* %s (%h)\" ${from}...${to}"
  },
  "github": {
    "release": true,
    "releaseName": "Version ${version}"
  }
}